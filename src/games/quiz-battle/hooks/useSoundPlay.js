export function useSoundPlay() {
  const playCorrect = () => { /* Play sound */ };
  const playWrong = () => { /* Play sound */ };
  const playFinish = () => { /* Play sound */ };

  return { playCorrect, playWrong, playFinish };
}
